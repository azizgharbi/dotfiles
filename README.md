#### Requirements:

- Install `lua` and `luaRocks`.
- Install `nvim`.

#### MacOS:

- Install `brew`.
- install `yabai` and `skhd`.
- Run the `install.lua`.

#### Linux:

- install `awesome wm`.
- Run the `install.lua`.


- At the end of the day you're another day older.
